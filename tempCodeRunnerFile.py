        # for i in range(size):
        #     index = startTime[i][0]
        #     start = startTime[i][1]
        #     duration = length[i][1]
        #     gnt.broken_barh([(start, duration)], (10, 10), facecolors =(colors[index]))
           
        # self.canvas.draw()
        # gnt.legend(processName) 